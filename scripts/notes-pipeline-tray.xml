<?xml version="1.0" encoding="UTF-8"?>
<!--
  Fallback Task Scheduler XML for notes-pipeline-tray.

  PRIMARY install path: scripts/install.ps1 builds the task programmatically via
  Register-ScheduledTask, which auto-resolves the current user's SID. Use this
  XML only as a backup or for manual `schtasks /Create /TN notes-pipeline-tray
  /XML notes-pipeline-tray.xml /F` registration.

  Notes:
    - <UserId> below is intentionally a placeholder. Replace with the actual
      SID (run `whoami /user`) or with `DOMAIN\username` before importing.
    - Settings mirror what install.ps1 generates.
-->
<Task version="1.4" xmlns="http://schemas.microsoft.com/windows/2004/02/mit/task">
  <RegistrationInfo>
    <Description>Launches the notes-pipeline voice-note tray app at user logon.</Description>
    <URI>\notes-pipeline-tray</URI>
  </RegistrationInfo>
  <Triggers>
    <LogonTrigger>
      <Enabled>true</Enabled>
    </LogonTrigger>
  </Triggers>
  <Principals>
    <Principal id="Author">
      <!-- Replace S-1-5-21-XXXX with current user's SID, or use UserId="DOMAIN\username". -->
      <UserId>S-1-5-21-PLACEHOLDER</UserId>
      <LogonType>InteractiveToken</LogonType>
      <RunLevel>LeastPrivilege</RunLevel>
    </Principal>
  </Principals>
  <Settings>
    <MultipleInstancesPolicy>IgnoreNew</MultipleInstancesPolicy>
    <DisallowStartIfOnBatteries>false</DisallowStartIfOnBatteries>
    <StopIfGoingOnBatteries>false</StopIfGoingOnBatteries>
    <AllowHardTerminate>true</AllowHardTerminate>
    <StartWhenAvailable>true</StartWhenAvailable>
    <RunOnlyIfNetworkAvailable>false</RunOnlyIfNetworkAvailable>
    <IdleSettings>
      <StopOnIdleEnd>false</StopOnIdleEnd>
      <RestartOnIdle>false</RestartOnIdle>
    </IdleSettings>
    <AllowStartOnDemand>true</AllowStartOnDemand>
    <Enabled>true</Enabled>
    <Hidden>false</Hidden>
    <RunOnlyIfIdle>false</RunOnlyIfIdle>
    <WakeToRun>false</WakeToRun>
    <ExecutionTimeLimit>PT0S</ExecutionTimeLimit>
    <Priority>7</Priority>
    <RestartOnFailure>
      <Interval>PT1M</Interval>
      <Count>3</Count>
    </RestartOnFailure>
  </Settings>
  <Actions Context="Author">
    <Exec>
      <Command>cmd.exe</Command>
      <!-- Replace these with the absolute path to your clone of this repo. -->
      <Arguments>/c "C:\path\to\audio-transcriber-obsidian-notes\scripts\run_tray.bat"</Arguments>
      <WorkingDirectory>C:\path\to\audio-transcriber-obsidian-notes</WorkingDirectory>
    </Exec>
  </Actions>
</Task>
